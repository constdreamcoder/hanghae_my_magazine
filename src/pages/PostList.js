import React from "react";

// components
import Post from "../components/Post";

const PostList = (props) => {
  return (
    <React.Fragment>
      <Post></Post>
    </React.Fragment>
  );
};

export default PostList;
